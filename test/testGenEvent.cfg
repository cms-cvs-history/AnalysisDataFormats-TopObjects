process Analysis = {

	# initialize MessageLogger
   	  include "FWCore/MessageLogger/data/MessageLogger.cfi"
   	  replace MessageLogger.cout.threshold = "ERROR"

##### example ttbar file:
#       include "TopQuarkAnalysis/TopObjectProducers/test/RecoInput.cfi"
#	replace PoolSource.maxEvents = 5

#### example single top files:
        source = PoolSource {
          untracked vstring fileNames = {
#                'rfio:/castor/cern.ch/user/g/giamman/SingleTop/reco_simu_0_99_tq_tqb_lhc_mu_0.root' # generator: SingleTop
                'rfio:/castor/cern.ch/user/g/giamman/SingleTop/singletop_mu_madgraph.root' # generator: MadGraph
          }
          untracked int32 maxEvents = 20
        }


	# Construct the MC Generated ttbar or single top events.
	# You have to uncomment one of the two (but not both).
#	  include "TopQuarkAnalysis/TopEventProducers/data/TtGenEvtProducer.cff"
	  include "TopQuarkAnalysis/TopEventProducers/data/StGenEvtProducer.cff"


	module test = testGenEvent {
		string proc = "st" // choice between "tt" and "st"
	}

	path p = {makeGenEvt,test}

}